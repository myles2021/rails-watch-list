<h2><%= @list.name %></h2>

<% @list.movies.each do |movie| %>
  <li><%= movie.title %>
  <li><%= movie.overview %>
  <li><%= movie.poster_url %>
  <li><%= movie.rating %>
  <%= link_to "Delete bookmark",
              bookmark_path(@list),
              method: :delete,
              data: { confirm: "Are you sure?" } %>
<% end %>
<br>
<%= link_to "Watch lists", lists_path %> |
<%= link_to 'Create a bookmark', new_list_bookmark_path(@list) %> |
